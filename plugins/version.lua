do

function run(msg, matches)
  return سلام!من ربات ضداسپم امین هستم
    ورژن من 1 هست.
end

return {
  description = "Shows bot version", 
  usage = "!version: Shows bot version",
  patterns = {
    "^!version$"
  }, 
  run = run 
}

end
